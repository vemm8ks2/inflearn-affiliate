# 인프런 강의 추천 시스템 환경 변수 설정 예시

# ============================================================================
# Next.js 환경 변수 (.env.local에 설정)
# ============================================================================
# Supabase 설정 (프론트엔드 전용 - 클라이언트에서 접근 가능)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# ============================================================================
# Python 스크래핑 환경 변수 (.env에 설정)
# ============================================================================
# Supabase 설정 (서버 전용 - 절대 클라이언트에 노출하지 말 것)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# 스크래핑 설정
MAX_COURSES_PER_RUN=20
HEADLESS_MODE=false

# ============================================================================
# Phase 7: OpenAI AI 리뷰 생성 환경 변수
# ============================================================================
# OpenAI API 설정 (서버 전용 - 절대 클라이언트에 노출하지 말 것)
OPENAI_API_KEY=sk-proj-your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo
MAX_TOKENS=500
TEMPERATURE=0.7

# ============================================================================
# 사용 방법
# ============================================================================
# 1. Next.js 개발 환경:
#    이 파일을 .env.local로 복사하고 NEXT_PUBLIC_* 변수만 설정
#    예: cp .env.example .env.local
#
# 2. Python 스크래핑:
#    이 파일을 .env로 복사하고 Python 관련 변수 설정
#    예: cp .env.example .env
#
# 3. 보안 주의사항:
#    - .env와 .env.local은 절대 Git에 커밋하지 말 것 (.gitignore에 포함됨)
#    - SUPABASE_SERVICE_ROLE_KEY는 서버에서만 사용
#    - NEXT_PUBLIC_* 접두사가 있는 변수만 클라이언트에 노출 가능
